<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.id %>의 상품 관리</title>
    <link rel="stylesheet" href="/css/wholesale/style.css">
    <link rel="stylesheet" href="/css/wholesale/prod.css">
</head>
<body>
    <%- include('wholesale/partials/header') %>

    <div class="content">
        <div class="product-info">
            <h2><%= user.id %>의 상품</h2>
            <div class="prod-toggle">
                <button onclick="window.location.href='/wholesale/prod'">상품</button>
                <button onclick="window.location.href='/wholesale/opt'">옵션</button>
            </div>

            <% if (!prod.success) { %>
                <p class="error-msg"><%= prod.msg %></p>
            <% } else if (prod.prods.length === 0) { %>
                <p>prods가 비어있습니다.</p>
            <% } else { %>
                <table class="prod-table">
                    <thead>
                        <tr>
                            <th>상품코드</th>
                            <th>상품명</th>
                            <th>단가</th>
                            <th>최소주문수량</th>
                            <th>단위주문수량</th>
                            <th>옵션</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% prod.prods.forEach(function(item) { %>
                            <tr>
                                <td><%= item.id %></td>
                                <td><%= item.name %></td>
                                <td><%= item.price.toLocaleString() %></td>
                                <td><%= item.minCnt %></td>
                                <td><%= item.unitCnt %></td>
                                <td><%= item.opt || '-' %></td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
        </div>
        <div class="sidebar">
            <div class="user-profile">
                <p><%= user.id %></p>
                <p>도매처 코드: <%= user.code %></p>
                <button onclick="window.location.href='/logout'">로그아웃</button>
            </div>
            <div class="management-section">
                <div class="upload-section">
                    <h2>엑셀로 메뉴추가</h2>
                    <button class="example-file" onclick="window.location.href='/file/product'">예시파일</button>
                    <button class="upload-button">업로드</button>
                </div>
                <div class="upload-section">
                    <h2>엑셀로 옵션추가</h2>
                    <button class="example-file" onclick="window.location.href='/file/option'">예시파일</button>
                    <button class="upload-button">업로드</button>
                </div>
                <div class="management-actions">
                    <h2>관리</h2>
                    <button class="download">다운로드</button>
                    <button class="delete-all">전체삭제</button>
                </div>
            </div>
        </div>        
    </div>

    <%- include('wholesale/partials/footer') %>
</body>
</html>
